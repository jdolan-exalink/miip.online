{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "devCommand": "npm run dev",
  "env": {
    "GEMINI_API_KEY": "@gemini_api_key"
  },
  "routes": [
    {
      "src": "^/(?!assets).*",
      "dest": "/index.html"
    }
  ]
}
